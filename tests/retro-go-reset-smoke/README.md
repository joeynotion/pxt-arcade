Retro-Go reset smoke fixture.

This project increments a persistent boot counter using `settings`,
automatically calls `control.reset()` on the first few boots,
and then stays alive once the target boot count is reached.
